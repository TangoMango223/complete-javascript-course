// Statements and Expressions

3 + 4
1991
true && false && !false // Compare with normal spoken language

// Example:
if (23 > 10) {
    const str = '23 is bigger';
    console.log(str);
}

// Statements are full sentences - keywords and programming
// Expressions - the math behind it, like a = 3 + 4

// LEGAL - Template Literal expects statements, not expressions.
console.log(`I'm ${2037 - 1991} years old!`);

// // ILLEGAL:
// console.log(`I'm ${if 2037 - 1991 } years old!`);

// Variable:
const me = "Christine"
console.log(`My name is ${me}!`);
